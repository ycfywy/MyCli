# AI API Configuration
# 支持 OpenAI 兼容的 API (OpenAI, Claude, DeepSeek, 等)
API_KEY=your-api-key-here
API_BASE_URL=https://api.openai.com/v1
MODEL=gpt-4o
# 可选：当 API 不支持 /models 接口时，用这里配置 /model 的可选列表（逗号分隔）
MODELS=gpt-4o,gpt-4o-mini

# Optional
MAX_TOKENS=4096
TEMPERATURE=0.7
